# Private Makefile's scripts

This folder contains scripts which are used by the Makefile.

**YOU MUST NOT RUN THEM EXPLICITLY!**

Use appropriate Makefile's targets. Run `make help` for more information.
